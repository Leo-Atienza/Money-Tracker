<?xml version="1.0" encoding="utf-8"?>
<!--
  FIX Phase 1.9 — Android data-extraction policy.

  Android 12+ (API 31+) replaced the all-or-nothing `android:fullBackupContent`
  with two opt-in surfaces: `cloud-backup` (Google Drive auto-backup) and
  `device-transfer` (set-up-new-device wizard). Money Tracker stores PII —
  every expense, every category, every account balance — so we deny both.

  Combined with `android:allowBackup="false"` and
  `android:fullBackupContent="false"` in AndroidManifest.xml, this means:

  - `adb backup` produces an empty bundle for our package.
  - Google Drive auto-backup never captures our DB.
  - The "transfer to new phone" flow does NOT carry our DB across devices.
    Users must use the in-app `Backup & Restore` (passphrase-encrypted —
    Phase 6.3) to migrate.

  See:
  https://developer.android.com/about/versions/12/backup-restore#xml-changes
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
        <exclude domain="file" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
        <exclude domain="file" />
    </device-transfer>
</data-extraction-rules>
